from django.urls import path

urlpatterns = [
    # Link + sync views land here once the Enable Banking client is wired.
    # path('banking/link/', views.link_start, name='banking_link_start'),
    # path('banking/link/callback/', views.link_callback, name='banking_link_callback'),
    # path('banking/transactions/', views.transactions_view, name='banking_transactions'),
]
